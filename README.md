# ResearchCopilot 

An AI-powered research assistant for academic PDFs with source-backed answers and page references.

## Features

- Upload academic PDF documents
- Semantic search across document content
- AI-generated answers with page references
- Similarity scoring for search results

## Tech stack

- Python
- Streamlit
- PyMuPDF
- Sentence Transformers
- FAISS
- OpenAI (GPT-4o-mini)

## Setup

1. Clone the repo
2. Create a virtual environment: `python -m venv .venv`
3. Activate it: `.venv\Scripts\activate`
4. Install dependencies: `pip install -r requirements.txt`
5. Create a `.env` file with your OpenAI API key:
